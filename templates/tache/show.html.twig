{% extends 'base.html.twig' %}

{% block title %}Tache{% endblock %}

{% block body %}
    <h1>Tache</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tache.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tache.name }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ tache.content }}</td>
            </tr>
            <tr>
                <th>Create_at</th>
                <td>{{ tache.createAt ? tache.createAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>
    <a href="{{ path('app_liste_index') }}">Retourne à liste</a>
    <a href="{{ path('app_tache_edit', {'id': tache.id}) }}">edit</a>

    {{ include('tache/_delete_form.html.twig') }}
{% endblock %}
