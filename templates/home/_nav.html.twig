 <nav class="navbar navbar-expand-lg">
  <div class="container-fluid">
    <a class="navbar-brand" style="color: purple" href="#">TODO</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        {% if app.user %}
        <li class="nav-item">
          <a class="nav-link active" style="color: red" aria-current="page" href="{{ path('app_liste_index') }}">Ma Liste</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" style="color: purple" href="{{ path('app_logout') }}">Se Déconnceter</a>
        </li>
        {% else %}
        <li class="nav-item">
          <a class="nav-link" style="color: purple" href="{{ path('app_login') }}">S'Enregistrer</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" style="color: purple" href="{{path('app_register')}}">Créer un compte</a>
        </li>
      {% endif %}
      </ul>
    </div>
  </div>
</nav>